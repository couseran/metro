// src/lib/game/world/WorldConstants.ts
//
// Shared numeric constants for world/tile geometry.
// Imported by both the simulation and the renderer — do not put game-logic here.

/** Width and height of each tile in world-space pixels. */
export const TILE_SIZE = 16;

/** Width of a chunk in tiles. */
export const CHUNK_WIDTH = 16;

/** Height of a chunk in tiles. */
export const CHUNK_HEIGHT = 16;
